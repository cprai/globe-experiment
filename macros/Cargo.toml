# Internal proc-macro helper crate (derives cannot live in the crate that
# uses them). Path-only, never published.
[package]
name = "macros"
edition = "2024"

[lib]
proc-macro = true

[dependencies]
quote = "1"
syn = "2"
